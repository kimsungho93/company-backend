// 실행 모듈. 모든 도메인 모듈을 의존해도 되는 유일한 곳이고, 실행 가능한 jar 도 여기서만 나온다.
// @SpringBootApplication 을 도메인 모듈에 두면 그 모듈이 다른 도메인을 의존해야 해서 경계가 무너진다.
plugins {
    id 'org.springframework.boot'
}

dependencies {
    implementation project(':common')
    implementation project(':user')
    implementation project(':calendar')

    implementation 'org.springframework.boot:spring-boot-starter-webmvc'
    implementation 'org.springframework.boot:spring-boot-starter-security'
    // 엔트리포인트가 필터 계층에서 JSON 을 직접 써야 해서 ObjectMapper 타입이 필요하다.
    // Boot 4 는 jackson 도 슬라이스로 분리해서 webmvc 만으로는 컴파일 클래스패스에 없다.
    implementation 'org.springframework.boot:spring-boot-starter-jackson'
    runtimeOnly 'com.mysql:mysql-connector-j'

    testImplementation 'org.springframework.boot:spring-boot-starter-webmvc-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-data-jpa-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-security-test'
    testImplementation 'org.springframework.boot:spring-boot-testcontainers'
    testImplementation 'org.testcontainers:testcontainers-mysql'
}
